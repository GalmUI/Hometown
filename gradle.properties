org.gradle.jvmargs=-Xmx2G
org.gradle.daemon=false
org.gradle.caching=true
org.gradle.workers.max=4

# Single source of truth for Hometown's built artifact and mod metadata version.
# Every intentional playtest build with changed code/data must use a new version.
mod_version=0.10.8
